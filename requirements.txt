# ==============================================================================
# DexGen Python dependencies
# ==============================================================================
# Isaac Sim & Isaac Lab are installed separately via the Dockerfile.
# This file covers DexGen-specific packages only.
# ==============================================================================

# Mesh / geometry
trimesh>=4.0.0
scipy>=1.11.0
numpy>=1.24.0

# Deep learning (PyTorch is pre-installed in Isaac Sim image; listed for reference)
# torch>=2.2.0         # already in Isaac Sim base image
# torchvision>=0.17.0

# Dataset storage
h5py>=3.9.0

# Experiment tracking (optional but recommended)
tensorboard>=2.14.0

# Progress bars
tqdm>=4.66.0

# YAML config loading
pyyaml>=6.0

# Plotting / debugging
matplotlib>=3.7.0

# Formatting / CLI Output
prettytable>=3.9.0

# DexGraspNet dependencies (for --grasp_method dexgraspnet)
transforms3d
urdf-parser-py
pytorch3d               # requires CUDA-matched build; see README
# torchsdf             # install from: pip install torchsdf (or build from source)
# pytorch_kinematics   # installed via: pip install -e third_party/DexGraspNet/thirdparty/pytorch_kinematics/

# Isaac Lab Utilities
flatdict==4.0.1
einops
flaky
junitparser
pytest
pytest-mock
